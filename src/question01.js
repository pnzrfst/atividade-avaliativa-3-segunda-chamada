
import leia from "readline-sync"

/*Questão 01
Escreva um algoritmo de uma calculadora. Você deve solicitar ao usuário dois números e a operação que o mesmo deseja fazer. 
Após isso, o algoritmo deve realizar o calculo com base na operação e valores informados, e informar o resultado da operação.
var numero1 = leia.questionFloat("digite um numero: ");
var operacao = leia.questionInt(`informe a operacao desejada: "(1) adicao, (2) subtracao, (3) divisao, (4) multiplicacao" `);
var numero2 = leia.questionFloat("digite outro numero: ");

switch(operacao){
    case 1:
        var adicao = numero1 + numero2;
        console.log(`o resultado da expressao foi de ${adicao}`)
    break;
    case 2:
        var subtracao = numero1 - numero2;
        console.log(`o resultado da expressao foi de ${subtracao}`)
    break;
    case 3: 
        var divisao = numero1 / numero2;
        if(numero1 || numero2 === 0){
            console.log("nao e possivel dividir por zero.")
        }else{
            console.log(`o resultado da expressao foi de ${divisao}`)
        }
        
    break;
    case 4:
        var multiplicacao = numero1 * numero2
        console.log(`o resultado da expressao foi de ${multiplicacao}`)
    break;
    default:
        console.log("insira um operador valido.");
}
*/
